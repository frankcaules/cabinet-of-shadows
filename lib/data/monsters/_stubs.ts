import type { MonsterStub } from "../types";

// Phase 2 reduced the stub list to zero — all thirteen monsters are now full
// dossiers. Kept as an empty list so the import chain elsewhere remains stable
// and future authors (placeholders, draft entries) have a documented home.
export const STUBS: MonsterStub[] = [];
